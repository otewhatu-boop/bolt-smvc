# Development profile properties for local in-memory authentication
# Use: -Dspring.profiles.active=dev

# Dev credentials (only for local dev/test)
app.dev.user=devuser
app.dev.password=password

# Endpoint used to retrieve the system status list configured by OpenAPI
status.api.url=http://localhost/status.php
